using (ScriptAPI) {
    type = AddByte("Header");

    if (type == 0) {
        AddUnicodeString("Party Name");
        AddByte("Is Public Party");
        AddUInt("Maximum # in Party");		
    } else if (type == 1) {
        
    } else if (type == 2) {
        AddField("Unknown", 8);
        AddByte("Party Type");
        AddField("Unknown", 2);
        AddLong("Page Number");
    }
}
