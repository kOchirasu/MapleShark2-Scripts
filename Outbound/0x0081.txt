using (ScriptAPI) {
    f = AddByte("Function");
    AddByte("State");
    AddInt("ServerTick");
    if (f == 0) {
        DecodeCoordF("Position");
    } else if (f == 1) {
        // nothing
    }
}