using (ScriptAPI) {
    f = AddByte("function");

    switch ((int)f) {
        case 2: // buy item
            AddInt("ShopItemId");
            AddInt("Amount");
        case 9: // pay for restock
            AddInt("restock price");
        case 10: // Times up restock
    }
}