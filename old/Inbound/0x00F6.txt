using (ScriptAPI) {
    f = AddByte("Function");

    switch ((int)f) {
        case 0:
            count = AddShort("count");
            for (i = 0; i < count; i++) {
                StartNode("Entry " + i);
                AddInt("Index+1000");
                AddInt("QuestId?"); // Some id
                AddLong("Timestamp"); // Intervals of 5min (spawn time?)
                EndNode(false);
            }
    }
}