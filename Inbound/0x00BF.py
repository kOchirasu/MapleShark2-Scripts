''' USER_CONDITION_EVENT '''
from script_api import *

f = add_bool("function")
if f == 0:
    add_int("unknown")
elif f == 1:
    add_int("unknown")
    # "m_title", "m_desc", "attachment", "m_slot", "content"
