from script_api import *

# GlobalFactor
with Node("f5"): 
    add_byte("Unknown")
    for i in range(99):
        add_float("Unknown")

with Node("f6"): 
    add_byte("Unknown")
    for i in range(99):
        add_float("Unknown")

with Node("f7"): 
    add_byte("Unknown")
    for i in range(99):
        add_float("Unknown")

with Node("f0"): 
    add_byte("Unknown")
    for i in range(99):
        add_float("Unknown")

with Node("f1"): 
    add_byte("Unknown")
    for i in range(99):
        add_float("Unknown")

with Node("f2"): 
    add_byte("Unknown")
    for i in range(99):
        add_float("Unknown")

