from script_api import *

# GlobalFactor
with Node("f5"): 
    AddByte("Unknown")
    for i in range(99):
        AddFloat("Unknown")

with Node("f6"): 
    AddByte("Unknown")
    for i in range(99):
        AddFloat("Unknown")

with Node("f7"): 
    AddByte("Unknown")
    for i in range(99):
        AddFloat("Unknown")

with Node("f0"): 
    AddByte("Unknown")
    for i in range(99):
        AddFloat("Unknown")

with Node("f1"): 
    AddByte("Unknown")
    for i in range(99):
        AddFloat("Unknown")

with Node("f2"): 
    AddByte("Unknown")
    for i in range(99):
        AddFloat("Unknown")

