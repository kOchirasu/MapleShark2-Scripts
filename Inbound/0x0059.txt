using (ScriptAPI) {
    count = AddShort("Count");
    for (i = 0; i < count; i++) {
        size = AddShort("BufferSize");
        AddInt("ObjectId");
        AddByte("Unknown");
        DecodeCoordS("");
        StartNode("Movement");
        DecodeCoordS("Speed");
        AddShort("Unknown");
        AddByte("Unknown");
        AddShort("Animation");
        AddShort("Counter (per action)");
    }
}