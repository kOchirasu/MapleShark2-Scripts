// dungeon title id
// dungeon goal id
// fame challenge dungeon...?
using (ScriptAPI) {
    f = AddByte("Function");
    if (f == 1) {
        // ??
    } else if (f == 2) {
        AddByte("Unknown");
        AddInt("Unknown");
    } else {
        count = AddInt("count");
        for (i = 0; i < count; i++) {
            StartNode("DungeonRoom " + i);
            AddInt("Id");
            AddByte("Unknown"); // Eligible?
            EndNode(false);
        }
    }
}