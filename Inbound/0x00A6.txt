using (ScriptAPI) {
    f = AddByte("function");
    
    switch ((int)f) {
        case 6:
            count = AddInt("count");
            for (i = 0; i < count; i++) {
                AddInt("Id");
                AddLong("Timestamp");
            }
    }
}