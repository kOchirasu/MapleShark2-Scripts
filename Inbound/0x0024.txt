
using (ScriptAPI) {
    count = AddByte("Unknown");
	if (count == 0 || count == 4) {
		// 0 == Start List
		// 4 == End List
	} else if (count == 1) {
		AddInt("ItemId");
		AddLong("ItemUid");
		AddLong("Unknown");
		AddBool("Unknown");
	} else if (count == 2) {
		AddLong("Unknown");
	} else if (count = 3) {
		AddLong("Unknown");
		AddInt("Unknown");
	}
}