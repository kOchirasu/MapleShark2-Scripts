from script_api import *

add_byte("Unknown")
add_short("Unknown")
f = add_byte("Unknown")
add_int("Unknown")
if f == 0:
    add_unicode_str("UnknownStr")
else:
    add_int("Unknown")
    add_int("Unknown")
