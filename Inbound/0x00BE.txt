using (ScriptAPI) {
	f = AddByte("function");	
	AddByte("Unknown"); // 1
	
	switch ((int)f) {
		case 0: // close recall
		    // none
		case 1: // open recall
		    AddUnicodeString("PlayerName"); // player who used the scroll
		    AddInt("Unknown"); // 1
		    AddInt("MapId");
		    AddInt("Unknown"); // Random ticks? (Unknown)
		    AddLong("Timestamp");
	}
}