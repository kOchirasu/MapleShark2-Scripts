using (ScriptAPI) {
    f = AddByte("Function");
    
    switch ((int)f) {
        case 0:
            count = AddInt("count");
            for (i = 0; i < count; i ++) {
                AddInt("Unknown");
            }
            AddLong("Unknown");
            AddInt("Unknown");
        case 1:
            AddInt("Unknown");
    }
}