using (ScriptAPI) {
    f = AddByte("Function");
    
    switch ((int)f) {
        case 0:
            count = AddInt("count");
            for (i = 0; i < count; i++) {
                StartNode("Entry " + i);
                //AddUnicodeString("UnknownStr");
                // Data Depends on string
                EndNode(false);
            }
        case 1:
        case 2:
        case 3:
    }
}