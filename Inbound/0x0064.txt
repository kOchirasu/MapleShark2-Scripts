using (ScriptAPI) {
    f = AddByte("function");
    if (f == 0) {
        count = AddInt("Count");
        for (i = 0; i < count; i++) {
            AddLong("Unknown");
            AddInt("BanType?");
            AddLong("AccountId");
            AddUnicodeString("BlockReason");
            AddLong("StartDate");
            AddLong("EndDatei");
        }
    }
}