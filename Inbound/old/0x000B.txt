
using (ScriptAPI) {
    AddByte("Unknown (1)");
    AddInt("Unknown (1)");
    AddUnicodeString("Server Name");
    AddByte("Unknown (4)");
    count = AddShort("count");
    for (i = 0; i < count; i++) {
        AddUnicodeString("IPAddress");
        AddShort("Port");
    }
}
