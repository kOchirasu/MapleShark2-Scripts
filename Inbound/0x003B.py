''' MERET '''
from script_api import *

add_long("Merets")
add_long("Merets2")
add_long("GameMerets")
add_long("GameMerets2")
delta = add_long("DeductedMerets") # negative number
'''
if delta < 0:
    pass # s_msg_chatting_consume_merat
    if feature(206):
        pass # s_msg_chatting_consume_merat_message
else:
    pass # s_msg_take_merat
'''
